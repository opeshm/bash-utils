" vimrc 
"
" Show line number (relative)
set rnu

" Ignore cases searching
set ignorecase

" Disable backups
set nobackup
set nowb
set noswapfile

" Use spaces instead of tabs
set expandtab

" Be smart when using tabs ;)
set smarttab

" 1 tab == 2 spaces
set shiftwidth=2
set tabstop=2
